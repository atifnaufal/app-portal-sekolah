@extends('layouts.app')
@section('content')<div class="mb-4"><a href="{{ route('pengumuman.index') }}" class="text-decoration-none">&larr; Kembali</a><h1 class="h3 fw-bold mt-3">{{ $pengumuman->exists ? 'Edit Pengumuman Publik' : 'Tambah Pengumuman Publik' }}</h1><p class="text-secondary">Agenda, berita, dan informasi resmi sekolah.</p></div><div class="card form-card"><div class="card-body p-4"><form method="POST" action="{{ $pengumuman->exists ? route('pengumuman.update',$pengumuman) : route('pengumuman.store') }}" enctype="multipart/form-data">@csrf @if($pengumuman->exists) @method('PUT') @endif<div class="mb-3"><label class="form-label">Judul pengumuman</label><input name="judul" value="{{ old('judul',$pengumuman->judul) }}" class="form-control" required></div><div class="mb-3"><label class="form-label">Isi informasi</label><textarea name="isi" rows="5" class="form-control" required>{{ old('isi',$pengumuman->isi) }}</textarea></div><div class="row g-3"><div class="col-md-6"><label class="form-label">Tanggal agenda</label><input name="tanggal_acara" type="date" value="{{ old('tanggal_acara',$pengumuman->tanggal_acara?->format('Y-m-d')) }}" class="form-control"></div><div class="col-md-6"><label class="form-label">Gambar / banner</label><input name="gambar" type="file" accept="image/jpeg,image/png,image/webp" class="form-control"><div class="small text-secondary mt-1">Maksimal 5 MB</div></div></div><div class="form-check mt-4"><input name="is_landing" type="checkbox" value="1" class="form-check-input" id="is_landing" @checked(old('is_landing',$pengumuman->is_landing))><label for="is_landing" class="form-check-label">Tampilkan sebagai banner utama di Beranda</label></div><button class="btn btn-primary mt-4">{{ $pengumuman->exists ? 'Simpan perubahan' : 'Terbitkan' }}</button></form></div></div>@endsection
