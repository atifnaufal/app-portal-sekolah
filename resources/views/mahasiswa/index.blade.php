@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 fw-bold mb-1">Data Mahasiswa</h1><p class="text-secondary mb-0">Kelola seluruh data peserta didik.</p></div><a href="{{ route('mahasiswa.create') }}" class="btn btn-primary">+ Tambah mahasiswa</a></div><div class="card table-card"><div class="card-body"><form class="row g-2 mb-3"><div class="col-md-5"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari NIM atau nama..."></div><div class="col-auto"><button class="btn btn-outline-secondary">Cari</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>NIM</th><th>Nama</th><th>Gender</th><th>Jurusan</th><th>Kelas</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($mahasiswas as $mahasiswa)<tr><td>{{ $mahasiswa->nim }}</td><td class="fw-semibold">{{ $mahasiswa->nama }}<div class="small text-secondary">{{ $mahasiswa->email }}</div></td><td>{{ $mahasiswa->jenis_kelamin === 'L' ? 'Laki-laki' : 'Perempuan' }}</td><td>{{ $mahasiswa->jurusan->nama }}</td><td>{{ $mahasiswa->kelas->nama }}</td><td class="text-end"><a href="{{ route('mahasiswa.edit',$mahasiswa) }}" class="btn btn-sm btn-outline-primary">Edit</a><form class="d-inline" method="POST" action="{{ route('mahasiswa.destroy',$mahasiswa) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus data ini?')">Hapus</button></form></td></tr>@empty<tr><td colspan="6" class="text-center text-secondary py-4">Belum ada data mahasiswa.</td></tr>@endforelse</tbody></table></div>{{ $mahasiswas->links() }}</div></div>@endsection
