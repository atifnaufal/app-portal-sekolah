@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><div class="text-primary small fw-semibold">MANAJEMEN AKUN</div><h1 class="h3 fw-bold mb-1">Guru dan siswa</h1><p class="text-secondary mb-0">Setujui akun baru agar bisa login, atau nonaktifkan akses. Pengganti verifikasi email.</p></div><a href="{{ route('register') }}" class="btn btn-primary">+ Registrasi akun</a></div><div class="card table-card"><div class="card-body"><form class="mb-3"><div class="input-group"><input name="search" value="{{ request('search') }}" class="form-control" placeholder="Cari nama, NIK, atau email"><button class="btn btn-outline-secondary">Cari</button></div></form><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Nama / NIK</th><th>Role</th><th>Kelas</th><th>Status</th><th>Aksi</th></tr></thead><tbody>@forelse($users as $user)<tr><td><strong>{{ $user->name }}</strong><div class="small text-secondary">{{ $user->nik }} · {{ $user->email }}</div></td><td>{{ ucfirst($user->role) }}</td><td>{{ $user->kelas?->nama ?? '-' }}</td><td><span class="badge {{ $user->aktif ? 'text-bg-success' : 'text-bg-warning' }}">{{ $user->aktif ? 'Aktif' : 'Menunggu Persetujuan' }}</span></td><td>
    <button class="btn btn-sm btn-outline-primary" data-bs-toggle="collapse" data-bs-target="#edit-{{ $user->id }}">Edit</button>
    <form method="POST" action="{{ route('admin.user.toggle',$user) }}" class="d-inline">@csrf @method('PATCH')<button class="btn btn-sm {{ $user->aktif ? 'btn-outline-warning' : 'btn-success' }}">{{ $user->aktif ? 'Nonaktifkan' : 'Setujui' }}</button></form>
    <form method="POST" action="{{ route('admin.user.destroy',$user) }}" class="d-inline" onsubmit="return confirm('Apakah Anda yakin ingin menghapus akun ini secara permanen?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form>
</td></tr><tr class="collapse" id="edit-{{ $user->id }}"><td colspan="5"><form method="POST" action="{{ route('admin.user.update',$user) }}" class="row g-2">@csrf @method('PUT')<div class="col-md-2"><input name="nik" value="{{ $user->nik }}" class="form-control" placeholder="NIK" required></div><div class="col-md-3"><input name="name" value="{{ $user->name }}" class="form-control" required></div><div class="col-md-2"><input name="no_hp" value="{{ $user->no_hp }}" class="form-control" placeholder="No HP" required></div><div class="col-md-3"><input name="email" type="email" value="{{ $user->email }}" class="form-control" required></div><div class="col-md-2"><select name="role" class="form-select" required><option value="guru" @selected($user->role==='guru')>Guru</option><option value="siswa" @selected($user->role==='siswa')>Siswa</option></select></div><div class="col-md-4"><select name="kelas_id" class="form-select" required>@foreach($kelases as $kelas)<option value="{{ $kelas->id }}" @selected($user->kelas_id===$kelas->id)>{{ $kelas->nama }}</option>@endforeach</select></div><div class="col-auto"><button class="btn btn-primary">Simpan perubahan</button></div></form></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-4">Belum ada akun guru/siswa.</td></tr>@endforelse</tbody></table></div></div></div><script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>@endsection
