@extends('layouts.app')
@section('content')
<div class="d-flex justify-content-between align-items-end mb-4"><div><div class="text-primary small fw-semibold">TAGIHAN SPP</div><h1 class="h3 fw-bold mb-1">{{ isset($spp) ? 'Edit data SPP' : 'Tambah tagihan SPP' }}</h1><p class="text-secondary mb-0">Pilih siswa berdasarkan kelas, nama, dan NIK.</p></div><a href="{{ route('spp.index') }}" class="btn btn-outline-secondary">Kembali</a></div>
<div class="card form-card"><div class="card-body p-4"><form method="POST" action="{{ isset($spp) ? route('spp.update',$spp) : route('spp.store') }}">@csrf @if(isset($spp)) @method('PUT') @endif<div class="mb-4"><label class="form-label fw-semibold">Siswa</label><select name="siswa_id" class="form-select" required><option value="">Pilih siswa</option>@foreach($siswas as $siswa)<option value="{{ $siswa->id }}" @selected(old('siswa_id',$spp->siswa_id ?? '')==$siswa->id)>{{ $siswa->kelas?->nama ?? '-' }} · {{ $siswa->name }} · NIK {{ $siswa->nik }}</option>@endforeach</select></div><div class="row g-3"><div class="col-md-3"><label class="form-label">Bulan</label><input name="bulan" type="number" min="1" max="12" value="{{ old('bulan',$spp->bulan ?? '') }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Tahun</label><input name="tahun" type="number" value="{{ old('tahun',$spp->tahun ?? date('Y')) }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Nominal tagihan</label><input name="nominal" type="number" min="0" value="{{ old('nominal',$spp->nominal ?? '') }}" class="form-control" required></div><div class="col-md-3"><label class="form-label">Sudah dibayar</label><input name="dibayar" type="number" min="0" value="{{ old('dibayar',$spp->dibayar ?? 0) }}" class="form-control"></div></div><div class="mt-3"><label class="form-label">Jatuh tempo</label><input name="jatuh_tempo" type="date" value="{{ old('jatuh_tempo',isset($spp) && $spp->jatuh_tempo ? $spp->jatuh_tempo->format('Y-m-d') : '') }}" class="form-control"></div><button class="btn btn-primary mt-4">{{ isset($spp) ? 'Simpan perubahan' : 'Buat tagihan' }}</button></form></div></div>
@endsection
