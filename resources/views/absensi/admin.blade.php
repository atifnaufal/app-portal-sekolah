@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between align-items-end mb-4"><div><div class="text-primary small fw-semibold">MONITORING HARIAN</div><h1 class="h3 fw-bold mb-1">Absensi hari ini</h1><p class="text-secondary mb-0">Pantau kedatangan guru dan siswa.</p></div><a href="{{ route('notifikasi.index') }}" class="btn btn-outline-primary">Notifikasi</a></div><div class="row g-3 mb-4"><div class="col-md-4"><div class="card stat-card"><div class="card-body"><div class="small text-secondary">Total hadir</div><div class="display-6 fw-bold">{{ $absensis->count() }}</div></div></div></div><div class="col-md-4"><div class="card stat-card" style="border-left-color:#198754"><div class="card-body"><div class="small text-secondary">Guru hadir</div><div class="display-6 fw-bold">{{ $absensis->where('user.role','guru')->count() }}</div></div></div></div><div class="col-md-4"><div class="card stat-card" style="border-left-color:#fd7e14"><div class="card-body"><div class="small text-secondary">Siswa hadir</div><div class="display-6 fw-bold">{{ $absensis->where('user.role','siswa')->count() }}</div></div></div></div></div><div class="card table-card"><div class="card-body"><h2 class="h5 mb-3">Log kedatangan</h2><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Nama</th><th>Role</th><th>Kelas</th><th>Waktu</th><th>Status</th></tr></thead><tbody>@forelse($absensis as $absensi)<tr><td class="fw-semibold">{{ $absensi->user->name }}</td><td>{{ ucfirst($absensi->user->role) }}</td><td>{{ $absensi->kelas?->nama ?? '-' }}</td><td>{{ substr($absensi->waktu_masuk, 0, 5) }}</td><td><span class="badge text-bg-success">Hadir</span></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-4">Belum ada absensi hari ini.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
