@extends('layouts.portal')
@section('content')<header class="portal-top"><div class="eyebrow">INFORMASI KELAS</div><div class="hero-name mt-2">Pengumuman</div><div class="class-pill mt-3">{{ $user->kelas?->nama ?? 'Semua kelas' }}</div></header><main class="portal-content"><p class="text-secondary small mb-4">Informasi penting dari sekolah dan guru.</p>@forelse($pengumumans as $item)<article class="card soft-card mb-3"><div class="card-body"><div class="d-flex justify-content-between gap-3"><div><div class="small text-primary fw-bold">{{ $item->kelas?->nama ?? 'Semua kelas' }}</div><h2 class="h6 fw-bold mt-2">{{ $item->judul }}</h2></div><span class="small text-secondary text-nowrap">{{ $item->created_at->format('d/m') }}</span></div><p class="small text-secondary mb-0 mt-2" style="white-space:pre-line">{{ $item->isi }}</p><div class="small text-secondary mt-3">Oleh {{ $item->user->name }}</div></div></article>@empty<div class="card soft-card"><div class="card-body text-secondary">Belum ada pengumuman.</div></div>@endforelse</main>@endsection
