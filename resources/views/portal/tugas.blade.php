@extends('layouts.portal')
@section('content')<header class="portal-top"><div class="eyebrow">RUANG BELAJAR</div><div class="hero-name mt-2">Tugas sekolah</div><div class="class-pill mt-3">{{ $user->kelas?->nama ?? 'Belum ada kelas' }}</div></header><main class="portal-content"><p class="text-secondary small mb-4">Daftar tugas yang perlu diselesaikan untuk kelasmu.</p>@forelse($tugas as $item)<article class="card soft-card mb-3"><div class="card-body"><div class="d-flex justify-content-between align-items-start gap-3"><div><div class="small text-primary fw-bold">{{ $item->kelas->nama }}</div><h2 class="h6 fw-bold mt-2 mb-1">{{ $item->judul }}</h2></div><span class="badge rounded-pill text-bg-warning">{{ $item->batas_pengumpulan?->format('d M') ?? 'Terbuka' }}</span></div><p class="small text-secondary mt-2 mb-0">{{ $item->deskripsi ?: 'Tidak ada deskripsi tambahan.' }}</p><div class="small text-secondary mt-3">Dibuat oleh {{ $item->user->name }}</div></div></article>@empty<div class="card soft-card"><div class="card-body text-secondary">Belum ada tugas untuk kelasmu.</div></div>@endforelse</main>@endsection
