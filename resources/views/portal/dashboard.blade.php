@extends('layouts.portal')
@section('content')<header class="portal-top"><div class="d-flex justify-content-between align-items-start"><div><div class="eyebrow">PORTAL {{ strtoupper($user->role) }}</div><div class="hero-name mt-2">Halo, {{ $user->name }}</div><div class="class-pill mt-3">{{ $user->kelas?->nama ?? 'Semua kelas' }}</div></div><div class="avatar">{{ strtoupper(substr($user->name,0,1)) }}</div></div></header><main class="portal-content"><div class="d-flex justify-content-between align-items-center mb-3"><h1 class="section-title mb-0">Aktivitas belajar</h1><span class="text-secondary small">{{ now()->format('d M Y') }}</span></div><div class="row g-3 mb-4"><div class="col-6"><a href="{{ route('pengumuman.index') }}" class="card soft-card text-decoration-none text-dark h-100"><div class="card-body"><div class="icon-box mb-3">&#9993;</div><div class="h4 fw-bold mb-0">{{ $pengumumans->count() }}</div><div class="small text-secondary">Pengumuman</div></div></a></div><div class="col-6"><a href="{{ route('tugas.index') }}" class="card soft-card text-decoration-none text-dark h-100"><div class="card-body"><div class="icon-box mb-3" style="background:#e5f7ef;color:#198754">&#9745;</div><div class="h4 fw-bold mb-0">{{ $tugas->count() }}</div><div class="small text-secondary">Tugas aktif</div></div></a></div></div><div class="d-flex justify-content-between align-items-center mb-3"><h2 class="section-title mb-0">Pengumuman terbaru</h2><a href="{{ route('pengumuman.index') }}" class="small text-primary text-decoration-none">Lihat semua</a></div>@forelse($pengumumans as $item)<article class="card soft-card mb-3"><div class="card-body"><div class="small text-primary fw-bold">{{ $item->kelas_id ? ($user->kelas?->nama ?? 'Kelas') : 'Semua kelas' }}</div><h3 class="h6 fw-bold mt-2 mb-1">{{ $item->judul }}</h3><p class="small text-secondary mb-0">{{ Str::limit($item->isi, 100) }}</p></div></article>@empty<div class="card soft-card"><div class="card-body text-secondary small">Belum ada pengumuman.</div></div>@endforelse</main>@endsection
