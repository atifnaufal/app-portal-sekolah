@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 fw-bold mb-1">Tugas Sekolah</h1><p class="text-secondary mb-0">Tugas berdasarkan kelas masing-masing.</p></div>@if(session('user_role') === 'guru')<a class="btn btn-primary" href="{{ route('tugas.create') }}">+ Buat tugas</a>@endif</div><div class="row g-3">@forelse($tugas as $item)<div class="col-md-6"><article class="card table-card h-100"><div class="card-body"><div class="small text-primary fw-semibold">{{ $item->kelas->nama }}</div><h2 class="h5 mt-2">{{ $item->judul }}</h2><p class="text-secondary">{{ $item->deskripsi }}</p><div class="small">Batas: <strong>{{ $item->batas_pengumpulan?->format('d M Y') ?? 'Tidak ditentukan' }}</strong></div>@if(session('user_role') === 'guru' && $item->user_id === session('user_id'))<form class="mt-3" method="POST" action="{{ route('tugas.destroy',$item) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Hapus</button></form>@endif</div></article></div>@empty<div class="col-12"><div class="alert alert-light border">Belum ada tugas untuk kelas ini.</div></div>@endforelse</div>@endsection
