@extends('layouts.app')
@section('content')<div class="mb-4"><a href="{{ route('kelas.index') }}" class="text-decoration-none">&larr; Kembali</a><h1 class="h3 fw-bold mt-3">{{ $kelas->exists ? 'Edit Kelas' : 'Tambah Kelas' }}</h1></div><div class="card form-card"><div class="card-body p-4"><form method="POST" action="{{ $kelas->exists ? route('kelas.update',$kelas) : route('kelas.store') }}">@csrf @if($kelas->exists) @method('PUT') @endif<div class="row g-3"><div class="col-md-5"><label class="form-label">Nama kelas</label><input name="nama" value="{{ old('nama',$kelas->nama) }}" placeholder="Contoh: XII IPA 1" class="form-control" required></div><div class="col-md-3"><label class="form-label">Tingkat</label><input name="tingkat" type="number" min="1" max="6" value="{{ old('tingkat',$kelas->tingkat) }}" class="form-control" required></div><div class="col-md-4"><label class="form-label">Tahun ajaran</label><input name="tahun_ajaran" value="{{ old('tahun_ajaran',$kelas->tahun_ajaran) }}" placeholder="2025/2026" class="form-control" required></div></div><button class="btn btn-primary mt-4">Simpan</button></form></div></div>@endsection
