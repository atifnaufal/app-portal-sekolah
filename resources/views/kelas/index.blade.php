@extends('layouts.app')
@section('content')<div class="d-flex justify-content-between align-items-center mb-4"><div><h1 class="h3 fw-bold mb-1">Data Kelas</h1><p class="text-secondary mb-0">Kelola rombongan belajar dan tahun ajaran.</p></div><a href="{{ route('kelas.create') }}" class="btn btn-primary">+ Tambah kelas</a></div><div class="card table-card"><div class="card-body"><div class="table-responsive"><table class="table align-middle"><thead><tr><th>Nama kelas</th><th>Tingkat</th><th>Tahun ajaran</th><th>Mahasiswa</th><th class="text-end">Aksi</th></tr></thead><tbody>@forelse($kelases as $kelas)<tr><td class="fw-semibold">{{ $kelas->nama }}</td><td>{{ $kelas->tingkat }}</td><td>{{ $kelas->tahun_ajaran }}</td><td>{{ $kelas->mahasiswa_count }}</td><td class="text-end"><a href="{{ route('kelas.edit',$kelas) }}" class="btn btn-sm btn-outline-primary">Edit</a><form class="d-inline" method="POST" action="{{ route('kelas.destroy',$kelas) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger" onclick="return confirm('Hapus kelas ini?')">Hapus</button></form></td></tr>@empty<tr><td colspan="5" class="text-center text-secondary py-4">Belum ada kelas.</td></tr>@endforelse</tbody></table></div></div></div>@endsection
